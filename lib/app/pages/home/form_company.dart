import 'package:flutter/material.dart';

class FormCompanyPage extends StatelessWidget {
  const FormCompanyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Register of the Company'),
      ),
      body: Container(),
    );
  }
}